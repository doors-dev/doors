package test

/*
import (
	"testing"

	"github.com/doors-dev/doors"
)
func TestSimpleLoad(t *testing.T) {
	bro := newBro(
		doors.ServePage(func(pr doors.PageRouter[PathA], r doors.RPage[PathA]) doors.PageRoute {
			return pr.PageFunc(PageA)
		}),
	)
	defer bro.close()
	page := bro.page(t, "/")
	defer page.Close()
	h1Text := page.MustElement("h1").MustText()
	if h1Text != "Page A" {
		t.Fatal("header missmatch")
	}
} */
